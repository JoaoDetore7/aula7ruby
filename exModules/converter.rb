module Converter
    def inch_to_centimeters(measure)
        puts "A conversão de #{measure} polegadas em centimetros: #{measure * 2.54}"
    end

    def centimeters_to_inch(measure)
        puts "A conversão de #{measure} centimetros em polegadas: #{measure / 2.54}"
    end
end

